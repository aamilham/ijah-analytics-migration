import { Component } from '@angular/core';

@Component({
  selector: 'app-home',
  templateUrl: './home.component.html', // The HTML file that contains the template
  styleUrls: ['./home.component.css'], // The CSS file for the AboutComponent styles
  standalone: true,
})
export class HomeComponent {}
